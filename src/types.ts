export interface Destination {
  _id: string;
  name: string;
  description: string;
  location: string;
  country: string;
  horizontalImageUrl: string;
  verticalImageUrl: string;
  isVisited: boolean;
  user: string;
}
